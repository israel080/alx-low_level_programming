#include "main.h"
#include <stdio.h>

/**
 *_isdigit - function to check zero to 9
 *@c: check input function
 *Return: 1 if it is,otherwise always 0
 */

int _isdigit(int c)
{
	if ((c <= 48) && (c >= 57))
	{
		return (i);
	}
	return (0);
}
