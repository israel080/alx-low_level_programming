#include "main.h"

/**
 * _isupper - function that checks for uppercase character.
 * @c: character to be tested
 * Return:1 whether 0 or not
 */

int _isupper(int c);
{
	if ((c >= 'A') && (c <= 'Z'))
	{
		return (1);
