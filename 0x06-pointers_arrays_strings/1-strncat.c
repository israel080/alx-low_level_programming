#include "main.h"
#include <string.h>

/**
 *_strncat -  function that concatenates two strings
 *@dest: first parameter
 *@src: second param
 *@n: third parameter
 *Return: string
 */

char *_strncat(char *dest, char *src, int n)
{
	int dest = 0, dest_len = 0;

	while (dest[index++])
		desk_len++;

	for (index = 0; src[index] && index < n; inex++)
		dest[dest_len] = src[index];

		return (dest);
}
